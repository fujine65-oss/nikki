# nikki プロジェクト運用ルール

## チーム「結（ゆい）」介護記事シリーズ — 制作ワークフロー

### Google Drive 自動エクスポート

記事の校閲が完了し公開版が確定したら、以下を実行する：

1. Markdown記法を除去した **note用プレーンテキスト版** を作成する
   - `##` `**` `>` `---` `[[]]` 等のMarkdown記法をすべて除去
   - テーブルは箇条書き・インデント形式に変換
   - リスト `- ` は `・` に統一
   - YAMLフロントマター（メタデータ）は削除
   - 絵文字（📌💡⚠️🌱✅❌✍📮）はそのまま残す
2. Google Drive `結シリーズ_note公開用/` フォルダに **プレーンテキスト(.txt)のまま** アップロードする
   - **重要：`disableConversionToGoogleType: true` を必ず指定する**（Googleドキュメントに変換すると改行が消えてnoteにコピペできない）
   - ファイル名：`【公開○th】記事タイトル_note用.txt`（公開順が一目でわかるように）
3. 校閲の指摘ログを Google Drive `結シリーズ_管理資料/` フォルダにアップロードする
4. 企画台帳・経営会議録を更新した場合は `結シリーズ_管理資料/` に最新版をアップロードする

### エクスポートのタイミング（重要）

- **校閲完了後（公開版v*確定時）** にのみエクスポートする
- **校閲前の下書きはDriveに出さない**（品質事故防止）

### Google Drive フォルダ構成

```
マイドライブ/
├── 結シリーズ_note公開用/       ← 本人がnoteにコピペする用
│   ├── 【公開1st】記事タイトル_note用
│   ├── 【公開2nd】記事タイトル_note用
│   └── ...
└── 結シリーズ_管理資料/         ← 企画台帳・会議録・指摘ログ
    ├── 企画台帳_更新案
    ├── 経営会議録_YYYY-MM-DD_議題
    └── 指摘ログ_記事名
```

### Google Drive フォルダID（参照用）

- `結シリーズ_note公開用/` : `1tFtxMPCIB3P7nH0iSUiqNbY9nInmJQxH`
- `結シリーズ_管理資料/` : `1yb7X4_KqfvkPLzABY_j3lNyeTbUhP-ZX`
