# nikki プロジェクト運用ルール

## チーム「結（ゆい）」介護記事シリーズ — 制作ワークフロー

### note公開ワークフロー（iPhone／iPad）

noteのモバイル版は **Markdown形式の貼り付け** に対応している（HTML形式は不可）。
**noteアプリではなく、ブラウザ（Chrome／Safari）の note.com** から投稿すること。

#### 公開操作（1本5分）

1. Claude Codeに「記事○のMarkdownをコードブロックで出して（タイトルと本文を分けて）」と依頼
2. **タイトル**のコードブロック → コピーボタンをタップ → note.comのタイトル欄に貼り付け
3. **本文**のコードブロック → コピーボタンをタップ → note.comの本文欄に貼り付け
4. プレビュー確認 → 公開

#### Markdownの出力ルール

- YAMLフロントマター（メタデータ）は削除
- `# タイトル` は本文に含めない（タイトル欄に別途入力）
- 本文は `##` 見出しから開始
- `[[wikilink]]` → リンクテキストのみ残す
- テーブル → 箇条書きに変換（noteはテーブル非対応）
- 絵文字（📌💡⚠️🌱✅❌✍📮）→ そのまま残す

#### note公開の注意点

- **noteアプリからは投稿しない**（書式が反映されない）
- **HTML形式では投稿しない**（タグがそのまま表示される）
- 必ず **ブラウザ版 note.com** から投稿する

### note公開ワークフロー（PC）

PCからの場合は、Google DriveのHTMLファイルからコピペも可能。

1. Google Driveの `結シリーズ_note公開用/` から `.html` ファイルを開く
2. `Ctrl+A`（全選択）→ `Ctrl+C`（コピー）
3. note.comの新規記事画面 → タイトル入力 → 本文に `Ctrl+V`
4. プレビュー確認 → 公開

### エクスポート（Google Drive）

校閲完了後（公開版v*確定時）にのみ実行する。**校閲前の下書きはDriveに出さない**（品質事故防止）。

1. 校閲の指摘ログを `結シリーズ_管理資料/` にアップロード
2. 企画台帳・経営会議録を更新した場合は `結シリーズ_管理資料/` に最新版をアップロード

### Google Drive フォルダ構成

```
マイドライブ/
├── 結シリーズ_note公開用/       ← PC用HTMLファイル
│   └── ...
└── 結シリーズ_管理資料/         ← 企画台帳・会議録・指摘ログ
    └── ...
```

### Google Drive フォルダID（参照用）

- `結シリーズ_note公開用/` : `1tFtxMPCIB3P7nH0iSUiqNbY9nInmJQxH`
- `結シリーズ_管理資料/` : `1yb7X4_KqfvkPLzABY_j3lNyeTbUhP-ZX`
