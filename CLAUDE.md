# nikki プロジェクト運用ルール

## チーム「結（ゆい）」介護記事シリーズ — 制作ワークフロー

記事1本の全工程。**必ずこの順番**で進める。

```
① 分析（マーケティング社員）
   ↓  競合分析＋公開戦略＝「何を・誰に・どう差別化して書くか」を決める
② 制作（専門職）
   ↓  分析結果を踏まえて初稿を書く
③ 校閲（校閲・監査役）
   ↓  品質チェック＋指摘ログ
④ エクスポート（専門職）
   ↓  HTML変換＋Drive保存
⑤ 公開（本人）
   ↓  noteにコピペ公開（外向き操作＝本人のみ）
⑥ 反応観測（マーケティング社員）
      公開後の数値＋次回へのフィードバック
```

---

### ① 分析（マーケティング社員 — 制作前に必ず実施）

記事テーマが決まったら、**書き始める前に**競合分析と公開戦略を確定する。

#### 1a. 競合URL収集（Claude側・軽量）
- WebSearchで「記事テーマ＋関連キーワード」を検索
- 企業メディア上位8本＋note競合7本を選定

#### 1b. 競合分析実行（Gemini側・重量→Claudeトークン節約）
- 本人に「GASの ▶実行 を押してください」と依頼
- GASの場所：Drive `結シリーズ_管理資料/結_競合分析GAS_コード_v2（精度最大版）`
- 実行方法：script.google.com → `analyzeArticle1` or `analyzeCustom` を ▶実行
- GASが Gemini 2.5 Pro で2パス分析（個別深掘り→横断分析）を自動実行
- 結果は `結シリーズ_管理資料/競合分析_*.md` に自動保存される

#### 1c. 分析結果の読み取り＆制作指示書の作成（Claude側・軽量）
- DriveのMCPで `競合分析_*.md` を読む（要約テキストのみ＝数千トークン）
- 分析結果をもとに**制作指示書**を作成する：
  - 推奨タイトル（SEO観点＋差別化）
  - 推奨見出し構成（H1→H2→H3の流れ）
  - 競合との差別化ポイント（記事のどこに埋め込むか）
  - 推奨ハッシュタグ（noteで使う5〜10個）
  - 公開タイミング（季節性＋曜日・時間帯）
  - 読者ペルソナ（誰に向けて・どんな感情で読むか）

#### 新しいテーマで分析を回す方法
GASの `analyzeCustom` 関数で `theme` / `perspective` / `urls` を書き換えるだけ。
URLの収集はマーケティング社員が WebSearch で行い、GASに渡すURL一覧を準備する。

#### トークン節約の仕組み
```
Claude（軽）→ URL収集
Gemini 2.5 Pro（重）→ 15記事×2万文字の取得・2パス分析
Claude（軽）→ 要約だけ読んで制作指示書を作成
```
重い処理をGemini側に委譲し、Claudeは戦略判断だけに集中する（トークン90〜95%削減）。

---

### ② 制作（専門職 — 分析結果を踏まえて執筆）

マーケティング社員の**制作指示書**を受け取ってから初稿を書く。

- 指示書の推奨構成・差別化ポイントを反映する
- 制作の型（YMYL鉄則）は従来通り踏襲
- 新人ケアマネ向けはトーン変更（「先輩が後輩に語る」実践知）

---

### ③ 校閲（校閲・監査役）

- 確立した校閲の型（制度確認・数値ぼかし・ユマニチュード3前提等）を適用
- マーケ視点の修正（タイトル・見出し変更）が品質を損ねないかもチェック
- 指摘ログを作成

---

### ④ エクスポート（専門職 — 校閲完了後のみ）

校閲が完了し公開版が確定したら、以下を実行する：

1. Markdownから **note投稿用のHTMLファイル** を作成する
   - 変換ルール：
     - `# タイトル` → `<h1>`（本文先頭に参照用として入れる。noteでは記事タイトル欄に別途入力）
     - `## 見出し` → `<h2>`
     - `### 小見出し` → `<h3>`
     - `**太字**` → `<strong>`
     - `> 引用` → `<blockquote><p>`
     - `- リスト` → `<ul><li>`
     - 段落（空行区切り） → `<p>`
     - `---` 区切り線 → `<hr>`
     - テーブル → `<ul><li>` 等の箇条書きに変換（noteはテーブル非対応）
     - 絵文字（📌💡⚠️🌱✅❌✍📮）→ そのまま残す
     - `[[wikilink]]` → リンクテキストのみ残す
   - YAMLフロントマター（メタデータ）は削除
   - CSSは付けない（ブラウザデフォルト表示で十分）
   - `<head>` に `<meta charset="utf-8">` のみ指定
2. Google Drive `結シリーズ_note公開用/` フォルダにアップロードする
   - **重要：`disableConversionToGoogleType: true` を必ず指定する**（Googleドキュメントに変換させない）
   - ファイル名：`【公開○th】記事タイトル_note用.html`（公開順が一目でわかるように）
3. 校閲の指摘ログを Google Drive `結シリーズ_管理資料/` フォルダにアップロードする
4. 企画台帳・経営会議録を更新した場合は `結シリーズ_管理資料/` に最新版をアップロードする

**エクスポートの鉄則**：
- **校閲完了後（公開版v*確定時）** にのみエクスポートする
- **校閲前の下書きはDriveに出さない**（品質事故防止）

---

### ⑤ 公開（本人 — 外向き操作）

本人のnote公開操作（5ステップ・1本10分以内）：

1. Google Driveから `.html` ファイルを開く（ブラウザで自動表示される）
2. `Ctrl+A`（全選択）→ `Ctrl+C`（コピー）
3. noteの新規記事画面を開く → タイトル欄に記事タイトルを入力
4. 本文欄に `Ctrl+V`（貼り付け）→ 見出し・太字・引用・リストが書式ごと反映される
5. プレビュー確認 → 公開

---

### ⑥ 反応観測（マーケティング社員 — 公開後）

公開後は定期的に以下を確認・レポートする：
- noteのビュー・スキ・コメント数
- 想定と実際の差分分析（分析時の仮説 vs 実際の反応）
- 次回記事の制作指示書へのフィードバック

---

## Google Drive フォルダ構成

```
マイドライブ/
├── 結シリーズ_note公開用/       ← 本人がnoteにコピペする用（.htmlファイル）
│   ├── 【公開1st】記事タイトル_note用.html
│   ├── 【公開2nd】記事タイトル_note用.html
│   └── ...
└── 結シリーズ_管理資料/         ← 企画台帳・会議録・指摘ログ・競合分析結果
    ├── 企画台帳_更新案
    ├── 経営会議録_YYYY-MM-DD_議題
    ├── 指摘ログ_記事名
    ├── 競合分析_テーマ_YYYY-MM-DD.md  ← GAS自動生成
    └── 結_競合分析GAS_コード_v2（精度最大版）
```

## Google Drive フォルダID（参照用）

- `結シリーズ_note公開用/` : `1tFtxMPCIB3P7nH0iSUiqNbY9nInmJQxH`
- `結シリーズ_管理資料/` : `1yb7X4_KqfvkPLzABY_j3lNyeTbUhP-ZX`
